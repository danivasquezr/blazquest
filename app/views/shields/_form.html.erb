<%= form_for(@shield) do |f| %>
  <% if @shield.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shield.errors.count, "error") %> prohibited this shield from being saved:</h2>

      <ul>
      <% @shield.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :shield %><br>
    <%= f.text_field :shield %>
  </div>
  <div class="field">
    <%= f.label :origin %><br>
    <%= f.text_field :origin %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :surname_id %><br>
    <%= f.number_field :surname_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
